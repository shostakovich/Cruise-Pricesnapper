<%= form_for(@cruise) do |f| %>
  <% if @cruise.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@cruise.errors.count, "error") %> prohibited this cruise from being saved:</h2>

      <ul>
      <% @cruise.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="clearfix">
    <%= f.label :name %>
    <div class="input">
        <%= f.text_field :name, :placeholder => "Name der Kreuzfahrt", :class => "span6" %>
      </div>
  </div>
  <div class="clearfix">    
    <%= f.label :url, "Ehoi-Url" %>
    <div class="input">
       <%= f.text_field :url, :placeholder => "http://www.e-hoi.de/...", :class => "span6" %>
    </div>
  </div>
  <div class="actions">
    <%= f.submit :class => "btn primary" %>
  </div>
<% end %>
